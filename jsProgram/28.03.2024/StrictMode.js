"use strict";

// Strict mode is enabled for the code below

var x = 10;
console.log(x); // Output: 10

// In strict mode, you cannot create a variable without declaring it first
// The following line will throw a ReferenceError in strict mode==>y is not defined
y = 20;
